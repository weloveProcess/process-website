[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: 모든 경로를 index.html 로 (새로고침/딥링크 404 방지)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
